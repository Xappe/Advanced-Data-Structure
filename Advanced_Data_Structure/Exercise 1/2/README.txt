Faça um algoritmo (na Linguagem C) para ler o arquivo de entrada “Grafo.txt” (que
representa o grafo do Exercício 01) e construir as estruturas solicitadas no Exercício 01,
imprimindo-as na tela em seguida.

Obs.: Para o algoritmo, considere que será
utilizado qualquer grafo para testá-lo,
não somente esse do exercício,
considerando o formato apresentado do
arquivo de entrada.


Write an algorithm (in C language) to read the input file “Grafo.txt” (which
represents the graph from Exercise 01) and build the structures requested in Exercise 01,
printing them on screen afterwards.

Obs.: For the algorithm, consider that it will be
used any graph to test it,
not just the exercise,
considering the presented format of the
input file.